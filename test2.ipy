import numpy as np
# We note that jac.shape = (3, N, 3, M)
jac = np.ones((3, 64, 3, 25000))
x0, x1, x2 = jac[:,:,0], jac[:,:,1], jac[:,:,2]


#import cross
#%timeit np.cross(x0, x1, axisa=0, axisb=0, axisc=1)
#%timeit cross.cross(x0, x1, axisa=0, axisb=0, axisc=1)
#%timeit cross.cross2(x0, x1, axisa=0, axisb=0, axisc=1)
#%timeit cross.cross3(x0, x1, axisa=0, axisb=0, axisc=1)

jac = np.ones((3, 64, 3, 200000))
x0, x1, x2 = jac[:,:,0], jac[:,:,1], jac[:,:,2]

import cross
#%timeit np.cross(x0, x1, axisa=0, axisb=0, axisc=1)
#%timeit cross.cross(x0, x1, axisa=0, axisb=0, axisc=1)
%timeit cross.cross2(x0, x1, axisa=0, axisb=0, axisc=1)
%timeit cross.cross3(x0, x1, axisa=0, axisb=0, axisc=1)
